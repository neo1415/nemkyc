import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('should be defined', () => {
    // Basic sanity test - the app module should be importable
    expect(true).toBe(true);
  });
});
